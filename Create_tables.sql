CREATE TABLE sales (
       	     customer_ID VARCHAR(1),
	     order_date DATE,
	     product_ID int
	     );

CREATE TABLE members (
       	     customer_ID VARCHAR(1),
	     join_date TIMESTAMP
	     );

CREATE TABLE menu (
       	     product_ID INT,
	     product_name VARCHAR(5),
	     price INT
	     );
